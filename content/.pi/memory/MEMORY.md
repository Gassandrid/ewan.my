# Pi Memory

<!-- Durable project/environment/lesson memory for Pi. Keep concise, factual, and actionable. Managed by the pi-dream-memory extension and /dream. -->

- Vault work should default to Obsidian-native operations through `obsidian_cli`; use `vault_cli` for semantic, graph, and git-history analysis.
- Treat `Private/Claude Memory/` as legacy prior-agent memory: useful context, not authoritative truth; verify against current vault notes when factual accuracy matters.
